import React from "react";
import { ToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";

const Toast = ({ text }) => {
  return <ToastContainer style={{ fontWeight: "bold" }} />;
};

export default Toast;
